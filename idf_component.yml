## IDF Component Manager Manifest File
name: "micro-decoder"
version: "0.4.1"
description: "C++ audio decoding library with HTTP/HTTPS streaming, supporting FLAC, MP3, Opus, Vorbis, and WAV"
license: "Apache-2.0"
maintainers:
  - Kevin Ahrendt <kevin.ahrendt@openhomefoundation.org>
url: https://github.com/esphome-libs/micro-decoder
repository: https://github.com/esphome-libs/micro-decoder.git
dependencies:
  idf: ">=5.5"
  esphome/micro-flac:
    version: ">=0.1.1"
    rules:
      - if: "$CONFIG{MICRO_DECODER_CODEC_FLAC} == True"
  esphome/micro-mp3:
    version: ">=0.2.0"
    rules:
      - if: "$CONFIG{MICRO_DECODER_CODEC_MP3} == True"
  esphome/micro-opus:
    version: ">=0.3.5"
    rules:
      - if: "$CONFIG{MICRO_DECODER_CODEC_OPUS} == True"
  esphome/micro-vorbis:
    version: ">=0.1.0"
    rules:
      - if: "$CONFIG{MICRO_DECODER_CODEC_VORBIS} == True"
  esphome/micro-wav:
    version: ">=0.1.0"
    rules:
      - if: "$CONFIG{MICRO_DECODER_CODEC_WAV} == True"
files:
  exclude:
    - ".claude/**"
    - ".github/**"
    - ".clang-*"
    - ".clangd"
    - ".markdownlint.yaml"
    - ".pre-commit-config.yaml"
    - "CLAUDE.md"
    - "build/**"
    - "examples/**"
    - "host_examples/**"
    - "script/**"
